APP_NAME=app
TEST_DIR=${APP_NAME}/tests
GIT_URL=https://github.com/drnic/concourse-tutorial.git
THIS_MACHINE_IP_ADDRESS=10.185.251.49

APP_DOCKER_CONTAINER=${APP_NAME}

CONCOURSE_IP=${THIS_MACHINE_IP_ADDRESS}
CONCOURSE_PORT=8080
CONCOURSE_URL=http://${CONCOURSE_IP}:${CONCOURSE_PORT}

CONCOURSE_USER=test
CONCOURSE_PASS=test

CONCOURSE_DB_USER=concourse_user
CONCOURSE_DB_PASS=concourse_pass

DOCKER_REGISTRY_NAME=docker-registry
DOCKER_REGISTRY_PORT=5000
DOCKER_REGISTRY_IP=${THIS_MACHINE_IP_ADDRESS}
DOCKER_REGISTRY_ORIGIN=${DOCKER_REGISTRY_IP}:${DOCKER_REGISTRY_PORT}
DOCKER_REGISTRY_URL=http://${DOCKER_REGISTRY_ORIGIN}
